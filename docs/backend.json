
{
  "entities": {
    "EvaluationConfig": {
      "title": "Configuración de Evaluación",
      "description": "Configuración pedagógica de una evaluación, incluyendo criterios e instrumentos digitalizados.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "unidad_id": { "type": "string" },
        "periodo_id": { "type": "string" },
        "nombre": { "type": "string" },
        "tipo": { "type": "string", "description": "Tipo de instrumento: cotejo, rubrica, escala, quizz, etc." },
        "indicador_id": { "type": "string" },
        "indicador_codigo": { "type": "string" },
        "indicador_peso": { "type": "number" },
        "peso_instrumento": { "type": "number" },
        "puntaje_maximo": { "type": "number" },
        "configuracion_json": { "$ref": "#/entities/GamificationConfig" },
        "creado_el": { "type": "object" }
      }
    },
    "QuizQuestion": {
      "title": "Pregunta de Quizz",
      "description": "Pregunta técnica de alta exigencia generada para gamificación.",
      "type": "object",
      "properties": {
        "text": { "type": "string" },
        "options": { "type": "array", "items": { "type": "string" } },
        "correctIndex": { "type": "number" },
        "timeLimit": { "type": "number" },
        "criterionId": { "type": "string" }
      },
      "required": ["text", "options", "correctIndex"]
    },
    "GamificationConfig": {
      "title": "Configuración de Gamificación",
      "description": "Metadatos y banco de preguntas para el desafío interactivo.",
      "type": "object",
      "properties": {
        "strategy": { "type": "string", "enum": ["individual", "grupal", "quizz"] },
        "questions": { "type": "array", "items": { "$ref": "#/entities/QuizQuestion" } },
        "criteria": { "type": "array" }
      }
    },
    "GamificationSession": {
      "title": "Sesión de Gamificación",
      "description": "Registro de una sala de juego ejecutada con sus resultados finales.",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "room_code": { "type": "string" },
        "evaluacion_id": { "type": "string" },
        "unidad_id": { "type": "string" },
        "participantes": { "type": "array" },
        "finalizado_el": { "type": "string", "format": "date-time" }
      }
    }
  },
  "auth": {
    "providers": ["email", "google"]
  },
  "firestore": {
    "/evaluaciones_config/{evalId}": {
      "schema": "EvaluationConfig",
      "description": "Repositorio de instrumentos digitalizados y configuraciones de Quizz."
    },
    "/gamificacion_sesiones/{sessionId}": {
      "schema": "GamificationSession",
      "description": "Historial persistente de salas de juego y resultados sincronizados."
    }
  }
}
